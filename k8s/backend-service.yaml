apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: jobportal
    service: backend
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: jobportal
    service: backend
  type: ClusterIP
